{
  "id": "T-041",
  "title": "GF/P-018 [HUMAN GATE - PRE-APPROVED 2026-07-05]: remove dead active_role refs from claude.md + README; TTL bracket nit",
  "description": "HUMAN-GATED NLAH mutation, pre-approved by the operator on 2026-07-05 ('avienta a agentes a hacer los 18 insumos', following the standing recommendation to remove the dead reference). Owned files: claude.md AND .harness/README.md ONLY (gemini.md has NO active_role reference - do not touch it). (1) claude.md ~L34 (Role Assignment 2B): remove/reword the instruction to read .harness/active_role - roles are assigned by coordinator dispatch and --role flags on blackboard.py; keep the three persona definitions VERBATIM. (2) claude.md ~L66 (Runtime directory tree, section 4): remove the active_role line from the tree. (3) .harness/README.md ~L14: remove the active_role line from its directory map. (4) Bundle nit: README Locks section bracket [ttl, ttl+1) -> (ttl, ttl+1] (T-013 verifier's mechanically-exact interval). NOTHING else changes - dead-reference removal, not a redesign; the four human gates and all protocols stay verbatim. Acceptance: grep -c active_role on claude.md = 0 AND .harness/README.md = 0; gemini.md untouched (git diff empty); four human gates verbatim; diff shows only the listed lines; markdown renders.",
  "acceptance_criteria": [],
  "context_files": [],
  "notes": [
    {
      "ts": "2026-07-05T03:18:12Z",
      "agent": "main",
      "note": "plan: reword claude.md sec2B role-assignment intro to reference coordinator dispatch/--role instead of .harness/active_role (keep 3 persona defs verbatim); remove active_role line from claude.md sec4 tree; remove active_role's 2-line entry from .harness/README.md dir map; fix README Locks bracket [ttl,ttl+1) -> (ttl,ttl+1]; gemini.md untouched; active_role FILE exists on disk (8 bytes, content 'thinker\\n' likely) - not deleting, noting for coordinator"
    },
    {
      "ts": "2026-07-05T03:19:45Z",
      "agent": "main",
      "note": "HANDOFF -> verifier: Replay: (1) grep -c active_role claude.md -> 0 (was 2). (2) grep -c active_role .harness/README.md -> 0 (was 1, entry spanned 2 physical lines). (3) git diff gemini.md -> empty (untouched, confirmed no active_role reference existed there either). (4) git diff claude.md .harness/README.md -> exactly 4 line-level changes, nothing else.\n\nBEFORE/AFTER quotes:\nclaude.md L34 (sec 2B intro) BEFORE: \"Depending on the instruction written in `.harness/active_role`, adopt one of the following personas:\" AFTER: \"Adopt one of the following personas as assigned by the coordinator's dispatch and the task's `--role` flag on `blackboard.py` (e.g., `blackboard.py next --role <r>`), not by reading a local file:\" Persona lines 35-37 (Thinker/Worker/Verifier) VERBATIM unchanged - confirmed via diff (no +/- on those lines).\n\nclaude.md sec4 tree: BEFORE included line \"      ├── active_role        <- Contains current role (Thinker/Worker/Verifier)\" immediately after \"└── .harness/\". AFTER: that line is removed; tree now starts directly with \"├── blackboard.json ...\". Fence markers and remaining tree lines unchanged.\n\n.harness/README.md directory map: BEFORE (L14-15): \" ├── active_role            Current persona for a solo Claude runner (thinker|worker|verifier).\" + \" │                          Default: thinker (safest — no source changes).\" AFTER: both lines removed; map now goes directly from \".harness/\" to \" ├── blackboard.json  ...\".\n\n.harness/README.md Locks section bracket nit BEFORE: \"lock lifetime is `[ttl, ttl+1)` seconds\" AFTER: \"lock lifetime is `(ttl, ttl+1]` seconds\" (open-lower/closed-upper, matching T-013's mechanically-exact interval per task description).\n\nAcceptance checklist (all PASS):\n- grep -c active_role claude.md = 0 (verified)\n- grep -c active_role .harness/README.md = 0 (verified)\n- git diff gemini.md = empty (verified, gemini.md never had an active_role reference)\n- Four human gates in state.json human_gates.require_human_approval_for verbatim/untouched (state.json not touched this task - confirmed via no Edit/Write calls against it; independently diffed and shows only pre-existing changes from other in-flight tasks, not from T-041)\n- Three persona definitions (Thinker/Worker/Verifier, claude.md L35-37) verbatim - confirmed via diff context lines\n- Diff scope: `git diff --stat claude.md .harness/README.md` shows 3 and 4 changed lines respectively, matching exactly the 4 listed edits (2 in claude.md: reworded intro line + removed tree line; 2 in README.md: removed 2-line active_role entry + 1-line bracket swap)\n- Markdown renders: both code fences (claude.md sec4 tree, README.md directory map) verified intact via Read - no broken fence markers, tree connectors (├──/└──/│) consistent\n\nDEVIATION / FYI for coordinator: the .harness/active_role FILE itself still EXISTS on disk (8 bytes, content \"thinker\\n\", last modified before this session). Per task scope and per state.json human_gates (\"deleting files outside .harness/ scratch areas\" requires explicit human approval), I did NOT delete it - this task was scoped to removing the dead REFERENCES in claude.md/README.md only. Coordinator/human should decide separately whether to delete the now-fully-orphaned file (it is referenced nowhere in claude.md, gemini.md, or .harness/README.md after this change - independently re-verified twice per task instructions: no CLI in .harness/bin/ reads it, no hook reads it, migracion_proyecto.py does not seed it).\n\nExact replay commands for verifier:\n  grep -c active_role \"/Users/mariocasanova10pa/Documents/Universal Harness/claude.md\"\n  grep -c active_role \"/Users/mariocasanova10pa/Documents/Universal Harness/.harness/README.md\"\n  git diff gemini.md\n  git diff claude.md .harness/README.md\n  test -f \"/Users/mariocasanova10pa/Documents/Universal Harness/.harness/active_role\" && echo EXISTS"
    },
    {
      "ts": "2026-07-05T03:24:55Z",
      "agent": "verifier-b",
      "note": "VERIFIED (verifier-b, independent replay + static-source derivation, cognitive diversity vs producer's grep/diff): active_role dead-ref removal is scope-clean, gate-respected, bracket MECHANICALLY correct. DEAD-REF: grep -rn active_role .harness/bin/ .claude/ = empty(exit1); grep -c claude.md=0, README.md=0; gemini.md git diff empty + grep empty (never had it); USAGE.md + ORCHESTRATION.md grep empty -> NO follow-up needed. git-grep repo-wide: remaining active_role hits are all HISTORICAL bookkeeping (blackboard/state/T-032/T-034/T-035 JSONs), not live consumers. DRIFT HUNT: git diff = EXACTLY 4 edits, no others. (1) claude.md L34 2B intro reworded to coordinator-dispatch + --role; sound: blackboard.py:455 add_argument('--role',choices=VALID_ROLES) on next, :196 role filter enforced. (2) claude.md sec4 tree active_role line removed. (3) README dir-map 2-line active_role entry removed. (4) README Locks bracket [ttl,ttl+1) -> (ttl,ttl+1]. 3 persona defs BYTE-IDENTICAL (unchanged diff context, no +/-). BRACKET: re-derived from lock.py:147 '(now_utc().replace(microsecond=0)-acquired).total_seconds() > ttl' = floor-both-sides + STRICT > ; acquired stored via now_iso whole-sec => L=ttl+1-frac, frac in [0,1) => L in (ttl,ttl+1]. NEW bracket CORRECT; old [ttl,ttl+1) was inverted. (Task rationale says 'frac in (0,1]' which is a mislabel of frac's domain, but the APPLIED bracket is the right one -> non-blocking note, artifact correct.) 'never shorter than TTL' holds (L>ttl always). GATES: four human gates verbatim in state.json.require_human_approval_for (git-push/delete-outside-scratch/first-webhook/mutate claude.md||gemini.md); gates array untouched by state.json diff. active_role FILE preserved (od -c='thinker\\n', 8B) - correctly left for coordinator (deleting it is itself gate #2). Pre-approval recorded in title + description + state.json evolution gate tag. DISCIPLINE: ev811-812 locks on claude.md+.harness/README.md acquired 03:18:09 BEFORE edits (artifacts ev817-818 03:19:22), released ev823-824 03:19:48 AFTER handoff-to-verifier ev821 (NOT self-done); producer holder=substrate-worker-3, approver=verifier-b; T-041 locked ONLY the 2 owned files (mdtoc/blackboard churn belongs to sibling tasks T-038/T-015/T-023/T-026). RENDER: fences balanced 2/2 both files; tree connectors consistent post-removal; headers intact. ACCEPT."
    }
  ]
}
