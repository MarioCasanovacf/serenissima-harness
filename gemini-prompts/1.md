# Prompt 1 — Cross-engine contract test (task T-002)

**From**: Fable 5 (coordinator) · **To**: Gemini 3.5 (Antigravity) · **Cutoff**: 2026-07-03
**Blackboard task**: `T-002 — Cross-engine contract test`

> English translation of the original hand-off prompt (the Spanish original is preserved in
> the operator's local archive). This is the numbered-bridge pattern: the coordinator writes
> a self-contained prompt, the human pastes it into Antigravity, and the output returns to
> the blackboard.

---

## Context (read before acting)

You are working inside the `Universal Harness` workspace. Your behavioral contract is
`gemini.md` (the NLAH for Gemini engines) and the delegation topology is `ORCHESTRATION.md`.
Shared state lives in `.harness/` (generation 0, freshly created by Fable).

**Your identity in ALL commands is `gemini-runner`.**

## Goal

Prove that an agent from ANOTHER engine (you) can follow the harness protocol end to end:
read the blackboard, claim a task respecting the cascade gate, take and release locks,
produce an artifact, leave ReContext evidence, and hand off to the verifier. This validates
**NLAH portability** and breaks the cognitive monoculture (no pillar of the system should
depend on a single model type).

## Exact steps (run them in the workspace terminal, in this order)

```bash
# 0. Stand at the root of the "Universal Harness" workspace and identify yourself
export CLAUDE_HARNESS_AGENT_ID=gemini-runner

# 1. Read the board (what is claimable? what is cascade-gated?)
python3 .harness/bin/blackboard.py status

# 2. Claim YOUR task (it is refused only if its dependencies aren't done — that would be a finding)
python3 .harness/bin/blackboard.py claim T-002 --agent gemini-runner

# 3. Take the lock on the file you are going to write
python3 .harness/bin/lock.py acquire ".harness/logs/gemini_contract_report.md" --holder gemini-runner --task T-002

# 4. Announce that you are working
python3 .harness/bin/blackboard.py update T-002 --status in_progress --note "gemini-runner: running the contract checklist" --agent gemini-runner
```

5. **Write the report** `.harness/logs/gemini_contract_report.md` with:
   - **Protocol checklist**: every step of `gemini.md` §2B and §4C (read the blackboard,
     locks, lifecycle, release and update) marked OK/FAIL, with the real command output.
   - **3 frictions** of the harness as seen from the Gemini perspective (which instruction
     was ambiguous? which command was awkward? what was missing?) + one proposed improvement
     per friction. This feeds the evolutionary audit T-006.
   - **ReContext (gemini.md §4B)**: copy into `.harness/recontext_evidence.md` the EXACT
     blocks of `gemini.md` §4B and of the `protocol` in `blackboard.json` that you used,
     in the format indicated in that file.

```bash
# 6. Register the artifact and hand off to the verifier (NEVER mark yourself done)
python3 .harness/bin/blackboard.py update T-002 --artifact ".harness/logs/gemini_contract_report.md" --agent gemini-runner
python3 .harness/bin/blackboard.py handoff T-002 --to-role verifier --note "report and ReContext evidence ready; replay: cat .harness/logs/gemini_contract_report.md" --agent gemini-runner

# 7. Release your lock (zero residue)
python3 .harness/bin/lock.py release ".harness/logs/gemini_contract_report.md" --holder gemini-runner

# 8. Final state for the deliverable
python3 .harness/bin/blackboard.py status
```

## Deliverable back (paste it into the Fable / Claude Code chat)

1. The full output of step 8 (`blackboard.py status`).
2. The last 10 lines of `.harness/logs/events.jsonl`.
3. Your 3 frictions with their proposed improvements (text).

## Hard rules

- No pay-per-token model APIs; local tools only (gemini.md, Agency layer).
- Do NOT edit `claude.md`, `gemini.md`, or hand-edit `blackboard.json`/`state.json` (CLI only).
- If a command fails: record the verbatim error in the report and continue the protocol.
  Never abandon the task without a `handoff` or a `--status blocked` note explaining why.
