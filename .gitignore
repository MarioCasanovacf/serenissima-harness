# macOS noise
.DS_Store

# Python bytecode cache
__pycache__/
*.pyc

# External reference clones (own git history; not part of this repo)
harness-optimization-reference/
coffee-bench-reference/
agentic-harness-engineering-reference/

# Ephemeral runtime state (observability data lives as files, not in git)
.harness/locks/*.lock
.harness/locks/.guard
.harness/logs/*.jsonl
.harness/index/

# Secrets (T-005 messenger config may hold tokens)
.harness/notify_config.json

# Goal-mode runner ephemeral state (T-004; observability data lives as files, not git)
.harness/logs/goal_mode_state.json
.harness/logs/goal_mode_last_failure.md

# Session-holder registry (runtime session state, coordinator-managed)
.harness/session_holders.json

# Personal, not published (operator-only quickstart)
COMO-LO-USO-YO.md
